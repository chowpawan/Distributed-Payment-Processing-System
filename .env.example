# Copy to .env and fill in your values (never commit .env)
STRIPE_API_KEY=sk_test_your_key_here
DB_USER=payments
DB_PASSWORD=payments
REDIS_HOST=localhost
KAFKA_BOOTSTRAP=localhost:9092
WEBHOOK_HMAC_SECRET=change-me-in-prod
